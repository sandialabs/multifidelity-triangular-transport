# `NonHierarchicalTriangularMap`

The non-hierarchical map combines peer low-fidelity transports with learned
high-fidelity shift and scale terms, with optional trainable corrections to the
pretrained low-fidelity parent maps.

```{eval-rst}
.. autoclass:: mftt.NonHierarchicalTriangularMap
   :members:
   :show-inheritance:
```
