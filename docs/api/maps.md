# Map classes

The three primary map classes implement the single-fidelity, hierarchical, and
non-hierarchical MFTT transport constructions. Each class has a dedicated page
whose API is generated from its NumPy-style docstrings.

```{toctree}
:maxdepth: 1

maps/triangular-map
maps/hierarchical-triangular-map
maps/non-hierarchical-triangular-map
```
